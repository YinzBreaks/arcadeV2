# Build Contract - Resistance Arcade V2

Rules:
- Option A only: games load via iframe under /play/:gameId
- Credits/pass are server-enforced (no client source of truth)
- Ports are fixed (see PORTS.md)
- No dependency upgrades or broad refactors unless explicitly requested

Commands:
- npm install
- npm run dev
- npm run backend
- npm run dev:all
- npm run check
