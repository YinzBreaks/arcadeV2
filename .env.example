# --- PORT REGISTRY (DO NOT CHANGE WITHOUT UPDATING PORTS.md + BUILD_CONTRACT.md) ---
VITE_DEV_PORT=5173
VITE_PREVIEW_PORT=4173

API_PORT=8787
COINBASE_WEBHOOK_PORT=8788

# Frontend origin (for redirects/cancel urls). In prod, set to your real domain.
APP_ORIGIN=http://localhost:5173

# --- PUBLIC (client) env vars for Vite ---
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional: point frontend at your local API in dev.
VITE_API_BASE_URL=http://localhost:8787

# --- SERVER-ONLY secrets (NEVER COMMIT .env) ---
COINBASE_COMMERCE_API_KEY=
COINBASE_COMMERCE_WEBHOOK_SECRET=

# Treat these webhook events as "paid enough" to fulfill (recommended: confirmed only in prod).
COINBASE_FULFILL_ON_EVENTS=charge:confirmed,charge:pending
